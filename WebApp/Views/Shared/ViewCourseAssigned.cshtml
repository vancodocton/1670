@model IEnumerable<WebApp.Models.Course>
@using WebApp.Utils

@{
    ViewBag.Title = "ViewCourseAssigned";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CourseCategory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th> Action</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Name
            </td>
            <td>
                @item.CourseCategory.Name
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @Html.ActionLink("View Trainee", "ViewTrainee", "Trainer", new { courseId = item.Id},null)
            </td>
        </tr>
    }

</table>
